import { SpaRouterlinkDirective } from './spa-routerlink.directive';

describe('SpaRouterlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaRouterlinkDirective();
    expect(directive).toBeTruthy();
  });
});
